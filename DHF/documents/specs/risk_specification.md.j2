# {{ doc_type_name }} Specification

---

**Document Metadata**

| Field | Value |
|-------|-------|
| **Document ID** | {{ doc_type_code }}-SPEC |
| **Version** | {{ version }} |
| **Generated** | {{ generation_date }} |
| **Status** | {{ status }} |
| **Project** | {{ project_name }} |

---

## 1. Introduction

This document specifies the Risk Analysis for {{ project_name }}, in accordance with ISO 14971:2019
and IEC 62304 §4.2. It forms part of the Design History File (DHF).

### 1.1 Intended Use and User Population

**Intended use:** {{ project_name }} is used by qualified radiation therapy treatment planners to
create, review, and export radiation treatment structures for cancer patients.

**Intended users:** Radiation therapy treatment planners, dosimetrists, radiation oncologists.

**Intended environment:** Clinical workstation with modern browser (Chrome/Chromium), connected
to a hospital DICOMweb-compatible PACS or DICOM repository.

**Reasonably foreseeable misuse:**
- Using the system without qualified clinical oversight
- Relying on contour suggestions without independent verification
- Operating on patient data outside of a validated network environment

### 1.2 Risk Acceptability Criteria

Risk acceptability is determined using a 5×5 severity-probability matrix:

| | Negligible (1) | Minor (2) | Serious (3) | Critical (4) | Catastrophic (5) |
|---|---|---|---|---|---|
| **Improbable (1)** | Acceptable | Acceptable | Acceptable | ALARP | Unacceptable |
| **Remote (2)** | Acceptable | Acceptable | ALARP | Unacceptable | Unacceptable |
| **Occasional (3)** | Acceptable | ALARP | Unacceptable | Unacceptable | Unacceptable |
| **Probable (4)** | ALARP | Unacceptable | Unacceptable | Unacceptable | Unacceptable |
| **Frequent (5)** | Unacceptable | Unacceptable | Unacceptable | Unacceptable | Unacceptable |

**Severity:** Negligible (1) no patient impact → Catastrophic (5) death or permanent severe disability.

**ALARP:** Risk control required; residual risk must be justified by documented clinical benefit.

### 1.3 Scope

This specification covers all risks identified in {{ project_name }} as of {{ generation_date }}.

---

## 2. Risk Analysis

{% for item in items %}
### {{ loop.index }}. {{ item.id }}: {{ item.title }}

<div class="risk-section" markdown="1">

{% if item.status %}**Status**: <span class="status-{{ item.status }}">{{ item.status | upper }}</span>  {% endif %}
{% if item.category %}**Category**: {{ item.category }}  {% endif %}

#### Hazard

{{ item.hazard }}

#### Harm

{{ item.harm }}

#### Risk Assessment

| Metric | Value |
|--------|-------|
| **Severity** | {{ item.severity }} |
| **Probability** | {{ item.probability }} |
| **Risk Level** | {{ item.risk_level }} |

{% if item.related_requirements %}
#### Related Requirements

{% for req in item.related_requirements %}
- {{ req }}
{% endfor %}
{% endif %}

</div>

{% endfor %}

---

## 3. Risk Summary

### 3.1 Statistics

| Metric | Count |
|--------|-------|
| **Total Risks** | {{ items | length }} |
| **Approved** | {{ items | selectattr('status', 'equalto', 'approved') | list | length }} |
| **Draft** | {{ items | selectattr('status', 'equalto', 'draft') | list | length }} |

### 3.2 Risk Level Distribution

| Risk Level | Count |
|------------|-------|
| **High** | {{ items | selectattr('risk_level', 'equalto', 'High') | list | length }} |
| **Medium** | {{ items | selectattr('risk_level', 'equalto', 'Medium') | list | length }} |
| **Low** | {{ items | selectattr('risk_level', 'equalto', 'Low') | list | length }} |

---

## 4. Document Control

**Standard**: ISO 14971:2019
**Last Updated**: {{ generation_date }}

---

*Generated from DHF items in {{ project_name }}.*
