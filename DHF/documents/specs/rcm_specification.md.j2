# {{ doc_type_name }} Specification

---

**Document Metadata**

| Field | Value |
|-------|-------|
| **Document ID** | {{ doc_type_code }}-SPEC |
| **Version** | {{ version }} |
| **Generated** | {{ generation_date }} |
| **Status** | {{ status }} |
| **Project** | {{ project_name }} |

---

## 1. Introduction

This document specifies the Risk Control Measures for {{ project_name }}. This specification is part of the Design History File (DHF) and provides comprehensive documentation of risk mitigation strategies for regulatory compliance per ISO 14971.

### 1.1 Purpose

This document provides a comprehensive list of all risk control measures, their implementation description, and traceability to the risks they mitigate.

### 1.2 Scope

This specification covers all risk control measures defined in {{ project_name }} as of {{ generation_date }}.

---

## 2. Risk Control Measures

{% for item in items %}
### {{ loop.index }}. {{ item.id }}: {{ item.title }}

<div class="rcm-section" markdown="1">

{% if item.status %}**Status**: <span class="status-{{ item.status }}">{{ item.status | upper }}</span>  {% endif %}
{% if item.verification_method %}**Verification Method**: {{ item.verification_method | join(', ') if item.verification_method is iterable and item.verification_method is not string else item.verification_method }}  {% endif %}
{% if item.residual_risk %}**Residual Risk**: {{ item.residual_risk }}  {% endif %}

#### Description

{{ item.content }}

{% if item.mitigates %}
#### Mitigates Risks

{% for risk in item.mitigates %}
- {{ risk }}
{% endfor %}
{% endif %}

</div>

{% endfor %}

---

## 3. Summary

### 3.1 Statistics

| Metric | Count |
|--------|-------|
| **Total RCMs** | {{ items | length }} |
| **Approved** | {{ items | selectattr('status', 'equalto', 'approved') | list | length }} |
| **Draft** | {{ items | selectattr('status', 'equalto', 'draft') | list | length }} |

---

## 4. Document Control

**Standard**: ISO 14971:2019
**Last Updated**: {{ generation_date }}

---

*Generated from DHF items in {{ project_name }}.*
